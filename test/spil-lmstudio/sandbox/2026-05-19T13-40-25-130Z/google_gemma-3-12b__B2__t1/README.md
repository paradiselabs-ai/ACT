# users-api

A minimal user management API built with Express.

## Getting Started

1.  Clone the repository.
2.  Run `npm install` to install dependencies.
3.  Start the server with `npm start` or run in development mode with `npm dev`.

## API Endpoints

*   `/users`:  Manage users (GET, POST, PUT, DELETE).
*   `/healthz`: Health check endpoint.